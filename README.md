# Defqon Countdown

A simple static countdown page for DEFQON.1 2027.

## Files
- index.html — the full page with styling and countdown logic

## Run locally
Open index.html in a browser, or serve the folder with a simple static server.

Example:
```bash
python -m http.server 8000
```
Then open http://localhost:8000.
